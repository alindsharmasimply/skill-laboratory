[project]
name = "skill-laboratory"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "alindsharmasimply", email = "alindsharmasimply@gmail.com" }
]
requires-python = ">=3.12"
dependencies = []

[project.scripts]
skill-laboratory = "skill_laboratory:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "mypy>=2.3.1",
]
